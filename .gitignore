# ============================================
# EduQuiz – .gitignore
# ============================================
# IMPORTANTE: NO ignores ni borres del repo:
#   - composer.lock   → así "composer install" instala las mismas versiones de PHP
#   - package-lock.json → así "npm install" instala las mismas versiones de Node
# Si esos archivos están en el repo, tus compañeros tendrán las mismas versiones
# al clonar y ejecutar composer install / npm install.
# ============================================

*.log
.DS_Store
Thumbs.db

# Entorno y secretos (nunca subir .env al repo)
.env
.env.backup
.env.production
.env.local
.env.*.local

# Dependencias (cada quien instala con composer install / npm install)
/vendor
/node_modules

# Build y assets generados
/public/build
/public/hot
/public/storage
/public/vendor/

# Storage: claves, logs, cache (no subir al repo)
/storage/*.key
/storage/logs/*
/storage/pail
/bootstrap/cache/*

# PHPUnit y herramientas PHP
.phpactor.json
.phpunit.result.cache
/.phpunit.cache

# IDEs y editores (cada quien tiene su config local)
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Composer (credenciales locales)
/auth.json

# Laravel / Homestead (config local)
Homestead.json
Homestead.yaml

# npm / yarn (logs de depuración)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.yarn-integrity
.npm

# Plantillas/paquetes descargados que no forman parte del código
startbootstrap-sb-admin-2-gh-pages/
tailadmin-vuejs-1.0.0/
